# WiFi credentials for the weather app's live-forecast fetch.
# Copy this file to `.env` and fill in your network, then rebuild.
# ESP32-S3 is 2.4 GHz only — a 5 GHz-only SSID will not connect.
# `.env` is gitignored; never commit real credentials.
GEA_WIFI_SSID=
GEA_WIFI_PASSWORD=
